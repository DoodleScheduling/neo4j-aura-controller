---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: aurainstances.neo4j.infra.doodle.com
spec:
  group: neo4j.infra.doodle.com
  names:
    kind: AuraInstance
    listKind: AuraInstanceList
    plural: aurainstances
    singular: aurainstance
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              cloudProvider:
                description: CloudProvider specifies the cloud provider
                enum:
                - aws
                - gcp
                - azure
                type: string
              connectionSecret:
                description: |-
                  ConnectionSecret is a reference to a secret which will contain the connection details.
                  By default this will be ${metadataname}-connection
                properties:
                  name:
                    type: string
                type: object
              graphAnalyticsPlugin:
                description: GraphAnalyticsPlugin specifies the graph analytics plugin
                  configuration of the instance
                type: boolean
              interval:
                description: Interval at which the controller should reconcile the
                  instance
                type: string
              memory:
                description: Memory specifies the memory allocation (e.g., "1GB",
                  "8GB", "16GB")
                type: string
              neo4jVersion:
                description: Neo4jVersion specifies the Neo4j version
                type: string
              region:
                description: Region specifies the cloud region for the instance
                type: string
              secret:
                description: |-
                  Secret is a reference to a secret containing Aura API credentials
                  Expected keys: clientId, clientSecret
                properties:
                  name:
                    type: string
                type: object
              suspend:
                description: Suspend tells the controller to suspend reconciliation
                  for this instance
                type: boolean
              tenantID:
                description: TenantID specifies the Aura tenant/project ID
                type: string
              tier:
                description: Tier specifies the Neo4j Aura instance tier
                enum:
                - free-db
                - professional-db
                - business-critical
                - enterprise-db
                type: string
              timeout:
                description: Timeout used for upstream http requests
                type: string
              vectorOptimized:
                description: VectorOptimized specifies the vector optimization configuration
                  of the instance
                type: boolean
            required:
            - cloudProvider
            - neo4jVersion
            - region
            - secret
            - tenantID
            - tier
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionUri:
                description: ConnectionSecret is the secret name which contains the
                  connection details
                type: string
              instanceId:
                description: InstanceID is the Aura instance ID
                type: string
              instanceStatus:
                description: Status represents the current status of the Aura instance
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation reconciled
                  by the controller
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
